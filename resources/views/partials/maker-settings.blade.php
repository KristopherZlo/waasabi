<div class="settings-row"><label for="maker-skills">{{ __('waasabi.skills') }}</label><input class="input" id="maker-skills" name="skills" maxlength="400" placeholder="{{ __('waasabi.skills_placeholder') }}" value="{{ old('skills', $user->skills) }}"></div>
<div class="settings-row"><label for="maker-link">{{ __('waasabi.portfolio') }}</label><input class="input" type="url" id="maker-link" name="portfolio_url" maxlength="500" value="{{ old('portfolio_url', $user->portfolio_url) }}"></div>
<div class="settings-row"><input type="hidden" name="open_to_help" value="0"><label><input type="checkbox" name="open_to_help" value="1" @checked(old('open_to_help', $user->open_to_help))> {{ __('waasabi.available') }}</label></div>
<div class="settings-row"><label for="maker-featured">{{ __('waasabi.featured') }}</label><select class="input" name="featured_post_id" id="maker-featured"><option value="">{{ __('waasabi.no_featured') }}</option>@foreach($user->posts()->where('type', 'post')->latest()->get() as $ownPost)<option value="{{ $ownPost->id }}" @selected((int) old('featured_post_id', $user->featured_post_id) === $ownPost->id)>{{ $ownPost->title }}</option>@endforeach</select></div>
